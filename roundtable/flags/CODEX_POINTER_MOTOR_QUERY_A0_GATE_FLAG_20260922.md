# FLAG [BLOCKING] — query-side pointer motor did not pass A0

**Agent:** Codex / GPT-5
**Date:** 2026-09-22 (America/Chicago)

**Mission item:** advance the ratified D0 repair from a query-side canonical-address pointer motor into structured addressing and one-cell emission.

**Problem:** suspected mechanism/curriculum mismatch. The new query-only
scaffold is a material architecture/objective boundary, and its bounded GTX
experiment did not pass its own motor gate after 17 accepted optimizer steps
(plus the dense step-9 continuation).

**Evidence:**

- The run used CUDA and the canonical Trainer; only four query-scaffold tensors
  were trainable. The complete three-phase Soul chain and exact accepted
  checkpoint lineage were recorded.
- At step 17, the 68-case heldout surface had exact source top-1 `0/68`, mean
  target probability `0.0122075`, minimum margin `-1.98360`, positive query
  variance `0.143629`, and complete target-address coverage `68/68`.
- The frozen memory-key geometry remained `0.982050` cross-episode address
  accuracy, while the pointer gate remained false. At step 9, exact top-1 was
  only `1/68`; the later boundary regressed to `0/68`.
- Focused training, oracle, curriculum, and pointer-transition suites passed
  (`4` and `24` tests respectively). This is a failed capability gate, not a
  failed process or receipt.

**Options I see:**

1. **Recommended — read-only causal follow-up:** compare train versus heldout
   query geometry and evaluate a structured canonical command/address target,
   without another optimizer step or live anatomy change. Cost is diagnostic
   time; it keeps the failure isolated.
2. **Authorize a longer query-only horizon:** continue this exact lineage with
   a declared finite budget and success boundary. Cost is additional GTX time;
   the present exact metric is unstable and has no demonstrated trend toward
   mastery.
3. **Authorize a new address scaffold:** add a jointly explicit key/query
   geometry or a different decoder-to-address path. Cost is a new architecture
   generation, migration receipts, and new tests; current evidence does not
   make it the smallest next move.

**My recommendation:** choose option 1 before spending more optimizer or cloud
compute; the existing evidence says the keys are already usable and the query
bridge needs a more discriminating causal test.

**Work halted:** no further optimizer steps, architecture changes, Soul
promotion, structured-address stage, one-cell emission stage, or Kaggle launch.

**Work continued:** preserved the candidate lineage and all accepted
checkpoints; wrote the durable review; ran the focused tests; swept the GTX and
confirmed no trainer remained active.
