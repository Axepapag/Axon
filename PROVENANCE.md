# Axon Provenance

## Archive Sources

- `D:\AxonGliksbot` and `D:\axon7` are historical source archives.
- `D:\Axon` is the clean active repo.
- Archive material can inform design, but active files must match `docs/SOURCE_OF_TRUTH.md`.

## Promoted Active Lines

| Area | Current status |
|---|---|
| `substrate/` | Frozen 16D character alphabet remains bedrock. |
| `cores/` | Transformer core plus private soul inhale/attend/exhale path. |
| `training/trainer_slot.py` | Charfield-only trainer for exact 16D character state. |
| `curator/` | Dormant container, recovered corpus, and semantic-edge tooling. |
| `runtime/` | Tick loop, bus, and round-table orchestration. |

## Removed Lines

The rejected wide-slot adapter lineage has been removed from active code, tests, generated adapter artifacts, and stale roundtable notes. It is not an active architecture, fallback, or compatibility layer in this repo.

## Policy

Do not reintroduce a compressed exact-text read path without an explicit source-of-truth update and Jeff's approval. Exact text must remain visible to cores as ordered frozen character cells.
